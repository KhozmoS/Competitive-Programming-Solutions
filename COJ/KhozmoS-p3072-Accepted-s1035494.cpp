#include<bits/stdc++.h>

using namespace std;
  int n ;
  //string cad;
int main()
{
   ios_base::sync_with_stdio(0);cin.tie(0);
   cin>>n;
   if(n == 2) {cout<<"NO\n"; return 0;}
    if( n % 2 == 0){
    // if((n / 2) % 2 == 0)
      cout<<"YES\n";
     // else
      // cout<<"NO\n";
    }
    else
       cout<<"NO\n";

    return 0;
}
