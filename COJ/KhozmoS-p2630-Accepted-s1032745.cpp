#include <bits/stdc++.h>

using namespace std;


int main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);

   cout<<"If a size-1 robot went on a rampage, we would have to destroy at least 1 robots.\n";
   cout<<"If a size-2 robot went on a rampage, we would have to destroy at least 3 robots.\n";
   cout<<"If a size-3 robot went on a rampage, we would have to destroy at least 19 robots.\n";
   cout<<"If a size-4 robot went on a rampage, we would have to destroy at least 1659 robots.\n";
   cout<<"If a size-5 robot went on a rampage, we would have to destroy at least 71733501 robots.\n";
    return 0;
}
